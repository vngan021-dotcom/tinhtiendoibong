# ===============================
# QUẢN LÝ TIỀN QUỸ ĐỘI BÓNG
# ===============================

# Bước 1: Nhập danh sách thành viên và số tiền hiện có
team = {}

n = int(input("Nhập số lượng thành viên trong đội: "))

for i in range(n):
    name = input(f"Nhập tên thành viên thứ {i+1}: ")
    money = float(input(f"Nhập số tiền hiện có của {name}: "))
    team[name] = money

print("\n✅ Danh sách ban đầu:")
for name, money in team.items():
    print(f"- {name}: {money:.2f} VND")

# ===============================
# Bước 2: Xử lý sau mỗi trận đấu
# ===============================
while True:
    choice = input("\nBạn có muốn cập nhật sau một trận đấu không? (y/n): ").lower()
    if choice == 'n':
        break

    total_cost = float(input("Nhập tổng chi phí trận đấu: "))
    num_people = int(input("Có bao nhiêu người tham gia trận này: "))

    if num_people == 0:
        print("⚠️ Không thể chia cho 0 người.")
        continue

    cost_per_person = total_cost / num_people
    print(f"Mỗi người sẽ chia: {cost_per_person:.2f} VND")

    # Cập nhật tiền còn lại cho mỗi người tham gia
    for i in range(num_people):
        name = input(f"Nhập tên người tham gia thứ {i+1}: ")
        if name in team:
            team[name] -= cost_per_person
        else:
            print(f"⚠️ {name} không có trong danh sách đội!")

    # In lại số tiền còn lại
    print("\n💰 Số tiền còn lại sau trận đấu:")
    for name, money in team.items():
        print(f"- {name}: {money:.2f} VND")

# ===============================
# Bước 3: Kết thúc và lưu dữ liệu
# ===============================
print("\n✅ Cập nhật cuối cùng:")
for name, money in team.items():
    print(f"- {name}: {money:.2f} VND")

# Lưu dữ liệu vào file (tùy chọn)
with open("quy_doi_bong.txt", "w", encoding="utf-8") as f:
    for name, money in team.items():
        f.write(f"{name}: {money:.2f} VND\n")

print("\n📁 Dữ liệu đã được lưu vào file 'quy_doi_bong.txt'")
